# 2.5.x → 3.0 column-level mapping

The core deliverable of the source-connector / schema-mapping spike (Feature 412): one row per 2.x
column, stating its 3.0 destination (`table.column` in [`backend/db/schema.ts`](../../backend/db/schema.ts))
or an explicit **NO DESTINATION YET** marker. This is what any later importer feature (414/416/418/420)
codes its transform step against — it does not re-derive the mapping itself.

**Inputs**: [`2.5x-source-schema.md`](2.5x-source-schema.md) (Task 706 — the 2.x column inventory) and
[`2.5x-export-bundle-format.md`](2.5x-export-bundle-format.md) (Task 707) on the source side;
`backend/db/schema.ts` on the destination side, cross-checked against `backend/models/*.ts` wherever
the schema alone doesn't say how a column is actually populated (`pages.path` vs. `tree.folderPath`,
`users.auth`'s per-strategy shape, `assets.id`/`tree.id` sharing a primary key).

[`verify-mapping-doc.test.mjs`](verify-mapping-doc.test.mjs) enforces that every column documented in
`2.5x-source-schema.md`'s 18 table sections has a corresponding row below, and that the structural
transforms called out in the task description are stated explicitly — run
`node --test docs/migration/verify-mapping-doc.test.mjs` from the repo root.

## Read this first: five things that are not simple renames

1. **Every legacy integer/string PK becomes a fresh UUID.** Every table below carries this
   transform implicitly on its `id` (or `key`) column, so it is stated once here rather than
   repeated 18 times: an importer needs an old-id → new-UUID lookup table per 2.x entity for the
   whole run, exactly as `2.5x-source-schema.md`'s "Read this first" already establishes for the
   source side.
2. **Every 2.x row lands under one `siteId`.** 2.x has no multi-site concept at all — there is
   exactly one wiki per 2.x database. 3.0 makes `siteId` a required column on nearly every table
   (`pages`, `pageHistory`, `assets`, `tags`, `tree`, `navigation`, `blocks`, `storage`, …), so a
   straight 2.x → 3.0 import writes the _same_ target `siteId` (the one site created for this
   import) onto every single row it produces. There is no 2.x column this comes from — it is an
   import-time constant, not a mapped value.
3. **`assets` + `assetData` + `assetFolders` (three 2.x tables) collapse into one 3.0 `assets`
   row.** 3.0's `assets` table inlines the blob (`data`, `preview` — both `bytea`) directly rather
   than splitting it into a detail table the way 2.x's `assetData` did, and folder placement moves
   out of `assets` entirely into the unified `tree` table (see point 4) rather than staying as an
   `assetFolders` FK chain. `storageInfo` (jsonb) is new in 3.0 with no 2.x source at all — it
   records which storage-sync targets have a copy and is populated by the storage layer, not by an
   importer.
4. **2.x's two separate hierarchies (`pageTree`'s integer parent-pointer chain, `assetFolders`'
   own separate integer parent-pointer chain) both become paths in one 3.0 `tree` table**, keyed by
   `folderPath` (`ltree`) instead of a walked parent chain. A `tree` row's `id` is the _same_ UUID
   as the `pages.id` or `assets.id` it describes (confirmed in `backend/models/pages.ts`'s
   `addPage()`/`backend/models/assets.ts`'s asset-create path, both of which pass the just-inserted
   row's id straight into `tree.addPage`/`tree.addAsset`) — mirroring the exact shared-PK
   convention `2.5x-source-schema.md` already documents for 2.x's `pageTree.id = pages.id` /
   `assetData.id = assets.id`. Concretely: `pageTree.parent` (walk-the-chain) and `assetFolders.parentId`
   (a second, separate chain) both convert to one `tree.folderPath` ltree value per row; there is no
   column-to-column mapping for "parent" any more, only a recomputed materialized path.
5. **Per-module `config` blobs need key-by-key remapping, not a blind copy.** 2.x and 3.0 prop
   names inside `authentication.config` / `storage.config` are **not guaranteed to match** even for
   the same module `key`. Three modules were spot-checked directly against both engines'
   `definition.yml` (2.x fetched from `requarks/wiki` `server/modules/**/definition.yml` @ `main`;
   3.0 read from this repo's `backend/modules/**/definition.yml`) — see
   [Module config remapping](#module-config-remapping) below for the concrete mismatches found in
   each.

---

## pages

| 2.x `pages` column | 3.0 destination                                                                                                                                                                                           | Transform / notes                                                                                                                                                                                                                                                                                                                                      |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `id`               | `pages.id`, and `tree.id` (shared PK)                                                                                                                                                                     | new UUID; the same UUID is reused for this page's `tree` row                                                                                                                                                                                                                                                                                           |
| `path`             | `pages.path` (verbatim) **and** `tree.folderPath`+`tree.fileName` (split)                                                                                                                                 | 3.0 keeps a flat `path` column on `pages` itself _and_ derives the ltree hierarchy for `tree` from the same string — both are populated, not either/or                                                                                                                                                                                                 |
| `hash`             | `pages.hash`                                                                                                                                                                                              | direct                                                                                                                                                                                                                                                                                                                                                 |
| `title`            | `pages.title`, `tree.title`                                                                                                                                                                               | direct, written to both rows                                                                                                                                                                                                                                                                                                                           |
| `description`      | `pages.description`                                                                                                                                                                                       | direct                                                                                                                                                                                                                                                                                                                                                 |
| `isPrivate`        | **NO DESTINATION YET**                                                                                                                                                                                    | 3.0 `pages` has no `isPrivate`/private-namespace column at all — page-level privacy in 3.0 is expressed entirely through page-rule permissions (`read:pages` DENY/ALLOW/FORCEALLOW rules on `groups.rules`, see below), not a boolean+namespace pair. An importer must translate a 2.x private page into an equivalent rule rather than a column value |
| `isPublished`      | derived into `pages.publishState` (`'draft'` / `'published'` / `'scheduled'`)                                                                                                                             | 2.x's boolean + the `publishStartDate`/`publishEndDate` pair together decide which of the three 3.0 enum states applies (a `scheduled` page in 3.0 didn't exist as a distinct state in 2.x)                                                                                                                                                            |
| `privateNS`        | **NO DESTINATION YET**                                                                                                                                                                                    | see `isPrivate` above — the whole private-namespace concept has no 3.0 equivalent column                                                                                                                                                                                                                                                               |
| `publishStartDate` | `pages.publishStartDate`                                                                                                                                                                                  | direct (string → `timestamp`)                                                                                                                                                                                                                                                                                                                          |
| `publishEndDate`   | `pages.publishEndDate`                                                                                                                                                                                    | direct (string → `timestamp`)                                                                                                                                                                                                                                                                                                                          |
| `content`          | `pages.content`                                                                                                                                                                                           | direct                                                                                                                                                                                                                                                                                                                                                 |
| `render`           | `pages.render`                                                                                                                                                                                            | direct                                                                                                                                                                                                                                                                                                                                                 |
| `toc`              | `pages.toc`                                                                                                                                                                                               | direct (json → jsonb)                                                                                                                                                                                                                                                                                                                                  |
| `contentType`      | `pages.contentType`                                                                                                                                                                                       | direct                                                                                                                                                                                                                                                                                                                                                 |
| `createdAt`        | **NO DESTINATION YET**                                                                                                                                                                                    | 3.0 `pages` has no `createdAt` column (only `updatedAt`) — page creation time lives in `pageHistory` instead, as the `versionDate` of the row with `action = 'created'`. An importer synthesizes that first history row rather than a `pages` column                                                                                                   |
| `updatedAt`        | `pages.updatedAt`                                                                                                                                                                                         | direct                                                                                                                                                                                                                                                                                                                                                 |
| `extra`            | **NO DESTINATION YET**                                                                                                                                                                                    | no equivalent free-form `extra` bag on 3.0 `pages`; 2.x used this for editor-specific scratch data, which 3.0 covers per-editor via `pages.config`/`pages.scripts` instead — needs a per-editor spot-check, not a blanket rule                                                                                                                         |
| `editorKey`        | `pages.editor`                                                                                                                                                                                            | direct rename; 3.0 dropped the `editors` module-registry table entirely (no `editors` table in `backend/db/schema.ts`) — `editor` is just a plain string key now, no FK                                                                                                                                                                                |
| `localeCode`       | `pages.locale`                                                                                                                                                                                            | direct rename                                                                                                                                                                                                                                                                                                                                          |
| `authorId`         | `pages.authorId`                                                                                                                                                                                          | remapped through the users old-id → new-UUID table                                                                                                                                                                                                                                                                                                     |
| `creatorId`        | `pages.creatorId`                                                                                                                                                                                         | remapped through the users old-id → new-UUID table                                                                                                                                                                                                                                                                                                     |
| (none)             | `pages.ownerId`                                                                                                                                                                                           | **new in 3.0, no 2.x source** — 2.x has no separate "owner" concept distinct from author/creator; an importer defaults it to `creatorId`                                                                                                                                                                                                               |
| (none)             | `pages.alias`, `pages.icon`, `pages.password`, `pages.relations`, `pages.scripts`, `pages.historyData`, `pages.ratingScore`, `pages.ratingCount`, `pages.isBrowsable`, `pages.isSearchable`, `pages.tags` | **new in 3.0, no 2.x column source** — `pages.tags` is populated from `pageTags` (see [tags](#tags) below), not blank; the rest default to their schema defaults on import                                                                                                                                                                             |

**pageLinks has no 2.x-equivalent counterpart carried forward** — see the dedicated
[pageLinks](#pagelinks) section: it is a distinct **NO DESTINATION YET**, not folded into
`pages.relations` despite the superficially similar name.

## pageHistory

3.0's `pageHistory` keeps only a handful of columns at the top level and folds the rest of "the page
as it stood" into one `meta` jsonb blob (per the doc-comment on `backend/db/schema.ts`: "description,
icon, tags, publish state and dates, relations, scripts, config, editor and content type").

| 2.x `pageHistory` column   | 3.0 destination                                                         | Transform / notes                                                                                                                                                                                                                 |
| -------------------------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                       | `pageHistory.id`                                                        | new UUID                                                                                                                                                                                                                          |
| `path`                     | `pageHistory.path`                                                      | direct                                                                                                                                                                                                                            |
| `hash`                     | **NO DESTINATION YET**                                                  | no `hash` column on 3.0 `pageHistory` at all                                                                                                                                                                                      |
| `title`                    | `pageHistory.title`                                                     | direct                                                                                                                                                                                                                            |
| `description`              | `pageHistory.meta.description`                                          | folded into `meta`                                                                                                                                                                                                                |
| `isPrivate`                | `pageHistory.meta.isPrivate` (or dropped)                               | folded into `meta` if kept at all — see `pages.isPrivate` NO DESTINATION note; 3.0 has no live private-page concept for this to round-trip through later                                                                          |
| `isPublished`              | `pageHistory.meta.publishState`                                         | folded into `meta`, same draft/published/scheduled derivation as `pages.publishState`                                                                                                                                             |
| `publishStartDate`         | `pageHistory.meta.publishStartDate`                                     | folded into `meta`                                                                                                                                                                                                                |
| `publishEndDate`           | `pageHistory.meta.publishEndDate`                                       | folded into `meta`                                                                                                                                                                                                                |
| `action`                   | `pageHistory.action`                                                    | direct column survives; 2.x free-text values need mapping onto 3.0's four documented kinds (`created`/`updated`/`moved`/`deleted`)                                                                                                |
| `pageId`                   | `pageHistory.pageId`                                                    | remapped through the pages old-id → new-UUID table; **stays a non-FK plain column on both sides**, deliberately, so history outlives a deleted page                                                                               |
| `content`                  | `pageHistory.content`                                                   | direct                                                                                                                                                                                                                            |
| `contentType`              | `pageHistory.meta.contentType`                                          | folded into `meta`                                                                                                                                                                                                                |
| `createdAt`                | **NO DESTINATION YET**                                                  | no `createdAt` on 3.0 `pageHistory` — only `versionDate` (see next row); if `createdAt` and `versionDate` differed for a 2.x row (see below), the `createdAt` value has nowhere to go                                             |
| `editorKey`                | `pageHistory.meta.editor`                                               | folded into `meta`, renamed the same way `pages.editorKey` → `pages.editor` is                                                                                                                                                    |
| `localeCode`               | `pageHistory.locale`                                                    | direct rename                                                                                                                                                                                                                     |
| `authorId`                 | `pageHistory.authorId`                                                  | remapped through the users old-id → new-UUID table; 3.0 sets it `NULL` `ON DELETE SET NULL` instead of 2.x's plain FK-less column                                                                                                 |
| `versionDate`              | `pageHistory.versionDate`                                               | direct — copy the stored value verbatim (per `2.5x-source-schema.md`'s note: recomputing the self-join backfill semantics is unnecessary for a straight migration)                                                                |
| `extra`                    | `pageHistory.meta` (merged in)                                          | 2.x's own catch-all json blob merges straight into 3.0's catch-all `meta` blob                                                                                                                                                    |
| (none)                     | `pageHistory.changedFields`, `pageHistory.reason`, `pageHistory.siteId` | **new in 3.0, no 2.x source** — `changedFields` has no 2.x equivalent to diff against without recomputing it from consecutive rows; `reason` is a 3.0-only editor prompt; `siteId` is the import-time constant from point 2 above |
| (none — dropped by design) | —                                                                       | `render` is **deliberately not carried forward**: the 3.0 schema comment states it is intentionally not stored per-history-row (regenerable, not worth doubling storage) — this is a real product decision, not a gap to fill     |

## pageTree

2.x's `pageTree` (one row per page, integer parent-pointer chain) becomes rows of 3.0's `tree` table
with `type = 'page'`, sharing their `id` with the corresponding `pages.id` — see point 4 above.

| 2.x `pageTree` column | 3.0 destination                             | Transform / notes                                                                                                                                                               |
| --------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                  | `tree.id`                                   | same UUID as the corresponding `pages.id` (shared PK, same convention 2.x used)                                                                                                 |
| `path`                | `tree.folderPath` (ltree) + `tree.fileName` | structural transform: the slash-separated string splits into the ltree ancestor path plus the leaf file name                                                                    |
| `depth`               | **NO DESTINATION YET**                      | not stored in 3.0 — `nlevel(folderPath)` computes it from the ltree value on demand, so a stored `depth` column would be redundant, but no column exists to copy the value into |
| `title`               | `tree.title`                                | direct                                                                                                                                                                          |
| `isPrivate`           | **NO DESTINATION YET**                      | same as `pages.isPrivate` above — no 3.0 tree-level privacy flag                                                                                                                |
| `isFolder`            | `tree.type` (`'folder'` vs `'page'`)        | the boolean becomes one of the three values of 3.0's `treeType` enum for this row                                                                                               |
| `privateNS`           | **NO DESTINATION YET**                      | same as `pages.privateNS`                                                                                                                                                       |
| `parent`              | `tree.folderPath`                           | structural transform, not a column mapping: the walked integer parent chain is replaced by one materialized ltree path per row (point 4 above)                                  |
| `pageId`              | `tree.id`                                   | redundant with `id` once the shared-PK convention is adopted on the 3.0 side — both point at the same new page UUID                                                             |
| `localeCode`          | `tree.locale`                               | direct rename                                                                                                                                                                   |
| `ancestors`           | **NO DESTINATION YET**                      | not stored — ltree's `<@`/`@>` operators query ancestry directly off `folderPath`, so a stored ancestor-id array is redundant the same way `depth` is                           |

## pageLinks

**NO DESTINATION YET.** 3.0's `backend/db/schema.ts` has no `pageLinks` table, no outbound-link index
table of any kind, and no equivalent model. This is easy to conflate with `pages.relations` (a jsonb
column that does exist on 3.0 `pages`) because the names are adjacent in meaning, but they are not the
same feature: `pages.relations` is a small, **user-curated** list of "related page" buttons a page
author explicitly adds (position/label/caption/icon — see `frontend/src/components/PageRelationDialog.vue`),
not an **automatically rebuilt index of every outbound link found in the page's content**, which is
what 2.x's `pageLinks` is (rebuilt from content on every save, per `2.5x-source-schema.md`). There is
currently nowhere for 2.x's `pageLinks` rows to land; an importer either drops them (they are a
derived index, regenerable from `pages.content` once a 3.0-side link-indexing feature exists) or holds
them pending that feature.

| 2.x `pageLinks` column | 3.0 destination        | Transform / notes |
| ---------------------- | ---------------------- | ----------------- |
| `id`                   | **NO DESTINATION YET** | no target table   |
| `path`                 | **NO DESTINATION YET** | no target table   |
| `localeCode`           | **NO DESTINATION YET** | no target table   |
| `pageId`               | **NO DESTINATION YET** | no target table   |

## comments

**NO DESTINATION YET — the flagship case.** `backend/db/schema.ts` has no `comments` table, no
comments model, and no comments API route at all in this branch: comments are simply not built yet.
This is tracked by the parent Epic 341's sibling **Comments epic (#335)** in the roadmap — an entirely
separate epic, not a child of 341 — which owns designing the 3.0 comments schema from scratch. Every
column below is therefore unmapped, and stays that way until Epic 335 lands a schema an importer can
target; this task does not attempt to guess that shape.

| 2.x `comments` column | 3.0 destination        | Transform / notes                                                                                                                                           |
| --------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                  | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `content`             | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `createdAt`           | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `updatedAt`           | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `pageId`              | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `authorId`            | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `render`              | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `name`                | **NO DESTINATION YET** | blocked on Epic 335 (guest commenter display name)                                                                                                          |
| `email`               | **NO DESTINATION YET** | blocked on Epic 335 (guest commenter email)                                                                                                                 |
| `ip`                  | **NO DESTINATION YET** | blocked on Epic 335                                                                                                                                         |
| `replyTo`             | **NO DESTINATION YET** | blocked on Epic 335 (2.x's `0`-sentinel-for-top-level convention is itself something Epic 335's schema will need to either preserve or replace with `NULL`) |

Practical consequence for the spike's connector interface (Task 712): reading `comments` rows out of a
2.x source is straightforward (they're a flat table), but the `SourceConnector` write side has
nowhere to put them yet — the interface should surface this as a distinct entity a caller can choose
to skip, not silently drop.

## users

| 2.x `users` column | 3.0 destination                                                                               | Transform / notes                                                                                                                                                                                                                                                                                                                     |
| ------------------ | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`               | `users.id`                                                                                    | new UUID                                                                                                                                                                                                                                                                                                                              |
| `email`            | `users.email`                                                                                 | direct                                                                                                                                                                                                                                                                                                                                |
| `name`             | `users.name`                                                                                  | direct                                                                                                                                                                                                                                                                                                                                |
| `providerId`       | `users.auth[authModuleId].id`                                                                 | **selects which key of the `auth` jsonb map the rest of this row's auth fields land under** (see below), and for a non-local provider its own value becomes the `.id` field inside that map entry, alongside `.email` (confirmed in `backend/models/users.ts`'s external-login path: `auth[strategy.id] = { id: profile.id, email }`) |
| `password`         | `users.auth[authModuleId].password`                                                           | folded into the jsonb map under the key identified by `providerKey`/`authModuleId` (local auth only)                                                                                                                                                                                                                                  |
| `tfaIsActive`      | `users.auth[authModuleId].tfaIsActive`                                                        | folded into the jsonb map                                                                                                                                                                                                                                                                                                             |
| `tfaSecret`        | `users.auth[authModuleId].tfaSecret`                                                          | folded into the jsonb map                                                                                                                                                                                                                                                                                                             |
| `jobTitle`         | `users.meta.jobTitle`                                                                         | folded into the 3.0 `meta` jsonb blob (confirmed shape in `models/users.ts`'s `init()`: `meta: { location, jobTitle, pronouns }`)                                                                                                                                                                                                     |
| `location`         | `users.meta.location`                                                                         | folded into `meta`                                                                                                                                                                                                                                                                                                                    |
| `pictureUrl`       | **NO DESTINATION YET**                                                                        | 3.0 uses a boolean `users.hasAvatar` flag plus a binary blob in the separate `userAvatars` table (an uploaded-and-stored avatar), not a URL column — a 2.x external `pictureUrl` has no equivalent slot to land in without either fetching and re-storing the image or dropping the field                                             |
| `timezone`         | `users.prefs.timezone`                                                                        | folded into the 3.0 `prefs` jsonb blob (confirmed shape in `models/users.ts`'s `init()`: `prefs: { timezone, dateFormat, timeFormat, appearance, cvd }`)                                                                                                                                                                              |
| `isSystem`         | `users.isSystem`                                                                              | direct                                                                                                                                                                                                                                                                                                                                |
| `isActive`         | `users.isActive`                                                                              | direct                                                                                                                                                                                                                                                                                                                                |
| `isVerified`       | `users.isVerified`                                                                            | direct                                                                                                                                                                                                                                                                                                                                |
| `mustChangePwd`    | `users.auth[authModuleId].mustChangePwd`                                                      | folded into the jsonb map, **not** a top-level column on 3.0 `users` the way it was on 2.x                                                                                                                                                                                                                                            |
| `createdAt`        | `users.createdAt`                                                                             | direct                                                                                                                                                                                                                                                                                                                                |
| `updatedAt`        | `users.updatedAt`                                                                             | direct                                                                                                                                                                                                                                                                                                                                |
| `providerKey`      | selects the `authModuleId` key of `users.auth`                                                | 2.x's string key (`'local'`, `'google'`, …) must be resolved to the **UUID** of the matching 3.0 `authentication` row for this site — that UUID, not the string, is the property key used inside the `auth` jsonb map                                                                                                                 |
| `localeCode`       | **NO DESTINATION YET**                                                                        | no per-user UI-locale preference field found on 3.0 `users`/`meta`/`prefs` in `backend/db/schema.ts`; needs confirming against the frontend i18n boot sequence before assuming it is truly gone                                                                                                                                       |
| `defaultEditor`    | **NO DESTINATION YET**                                                                        | no per-user default-editor preference field found in 3.0's `users`/`meta`/`prefs`                                                                                                                                                                                                                                                     |
| `lastLoginAt`      | `users.lastLoginAt`                                                                           | direct                                                                                                                                                                                                                                                                                                                                |
| `dateFormat`       | `users.prefs.dateFormat`                                                                      | folded into `prefs`                                                                                                                                                                                                                                                                                                                   |
| `appearance`       | `users.prefs.appearance`                                                                      | folded into `prefs`                                                                                                                                                                                                                                                                                                                   |
| (none)             | `users.auth[authModuleId].restrictLogin`, `.tfaRequired`, `users.passkeys`, `users.hasAvatar` | **new in 3.0, no 2.x column source** — default per their schema defaults (`restrictLogin`/`tfaRequired` false, `passkeys` `{}`, `hasAvatar` false unless the importer separately migrates avatar blobs)                                                                                                                               |

## groups

**The rule-shape mismatch, verified.** The task description asks this to be checked rather than
assumed, since "2.x pageRules" and "3.0 groups.rules" are not guaranteed to share field names. They do
not. Confirmed by fetching 2.x's actual GraphQL schema
(`server/graph/schemas/group.graphql` @ `requarks/wiki` `main`) against 3.0's `GroupRule` TypeScript
interface (`backend/models/groups.ts`):

```
// 2.x PageRule (requarks/wiki server/graph/schemas/group.graphql)
type PageRule {
  id: String!
  deny: Boolean!          #  <-- a boolean, not a 3-state mode
  match: PageRuleMatch!   #  START | EXACT | END | REGEX | TAG   (no TAGALL)
  roles: [String]!
  path: String!
  locales: [String]!
}

// 3.0 GroupRule (backend/models/groups.ts)
interface GroupRule {
  id: string
  name: string                                          // new, no 2.x source
  roles: string[]
  match: 'START' | 'END' | 'REGEX' | 'TAG' | 'TAGALL' | 'EXACT'
  mode: 'ALLOW' | 'DENY' | 'FORCEALLOW'                 // <-- 3-state, replaces `deny`
  path: string
  locales: string[]
  sites: string[]                                        // new, no 2.x source
}
```

| 2.x `pageRules[]` field | 3.0 `groups.rules[]` field | Transform / notes                                                                                                                                                                                                                                                                            |
| ----------------------- | -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                    | `id`                       | can be carried forward verbatim (both are opaque strings) or regenerated — no cross-table FK depends on a rule's own id                                                                                                                                                                      |
| `deny`                  | `mode`                     | **value transform, not a rename**: 2.x `deny: true` → 3.0 `mode: 'DENY'`; 2.x `deny: false` → 3.0 `mode: 'ALLOW'`. **`mode: 'FORCEALLOW'` has no 2.x source at all** — 2.x never had a force-allow concept, so an imported rule set can only ever produce ALLOW/DENY rules, never FORCEALLOW |
| `match`                 | `match`                    | direct for all five 2.x values (`START`/`EXACT`/`END`/`REGEX`/`TAG`); 3.0's sixth value `TAGALL` has no 2.x source to map from either                                                                                                                                                        |
| `roles`                 | `roles`                    | direct — same page-permission strings on both sides (`read:pages`, `write:pages`, …)                                                                                                                                                                                                         |
| `path`                  | `path`                     | direct (including the TAG-match convention of storing a comma-separated tag list in this field instead of a path — same field reused for both purposes on both sides)                                                                                                                        |
| `locales`               | `locales`                  | direct                                                                                                                                                                                                                                                                                       |
| (none)                  | `name`                     | **new in 3.0, no 2.x source** — an importer synthesizes a label (e.g. from `match`+`path`) since 2.x rules were unnamed                                                                                                                                                                      |
| (none)                  | `sites`                    | **new in 3.0, no 2.x source** — defaults to `[]` (applies on every site), which is the correct behavior for a single-site 2.x import per point 2 above                                                                                                                                       |

Top-level `groups` columns:

| 2.x `groups` column | 3.0 destination                                          | Transform / notes                                                                                            |
| ------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `id`                | `groups.id`                                              | new UUID                                                                                                     |
| `name`              | `groups.name`                                            | direct                                                                                                       |
| `permissions`       | `groups.permissions`                                     | direct — same closed list of global-permission strings on both sides (see `CLAUDE.md`'s Permissions section) |
| `pageRules`         | `groups.rules`                                           | array reshaped per-element as above                                                                          |
| `isSystem`          | `groups.isSystem`                                        | direct                                                                                                       |
| `createdAt`         | `groups.createdAt`                                       | direct                                                                                                       |
| `updatedAt`         | `groups.updatedAt`                                       | direct                                                                                                       |
| `redirectOnLogin`   | `groups.redirectOnLogin`                                 | direct (added `2.5.12.js` on the 2.x side, present since the 3.0 baseline)                                   |
| (none)              | `groups.redirectOnFirstLogin`, `groups.redirectOnLogout` | **new in 3.0, no 2.x source** — default to `''` (schema default) on import                                   |

## userGroups

| 2.x `userGroups` column | 3.0 destination        | Transform / notes                                                                                                                                                                                        |
| ----------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                    | **NO DESTINATION YET** | 3.0's `userGroups` has no surrogate id at all — it's a composite-PK `(userId, groupId)` relation table, so the 2.x surrogate integer id has nothing to map to (it was never referenced by anything else) |
| `userId`                | `userGroups.userId`    | remapped through the users old-id → new-UUID table                                                                                                                                                       |
| `groupId`               | `userGroups.groupId`   | remapped through the groups old-id → new-UUID table, **except** for a `groupId` of `1` (2.x Administrators) or `2` (2.x Guests): those source group rows are never imported (`groups.isSystem: true` — an equivalent already exists in every 3.0 install), so this membership is instead remapped onto this install's real target admin/guest group id via `Groups.assignUserToGroup()` — see Task 414/731 |

**Re-run safety (Task 414/732):** the users/groups importer (`backend/migration/importers/users-groups.ts`)
detects a source `groups`/`users` row that was already imported by a prior run by matching on the
target's own natural key instead of a separately persisted old-id → new-UUID record — `name` for
`groups`, `email` for `users` (already unique). No extra id-mapping table or JSON artifact is
persisted: the matching target row's own primary key already supplies the id a persisted map would
have, so `importUserGroups()`'s translation works identically on a second run whether the id came from
a fresh insert or from this lookup. When a match is found, the row is never re-inserted; if the
freshly-converted candidate's content also matches what's stored, it's reported `existing`, otherwise
`diverged` — and a `diverged` row is deliberately **not** overwritten, to avoid silently discarding an
administrator's own in-3.0 edits to an already-imported group/user.

## assets

Collapses with `assetData` and `assetFolders` into one 3.0 `assets` row plus a paired `tree` row — see
point 3 and point 4 above.

| 2.x `assets` column | 3.0 destination                                                          | Transform / notes                                                                                                                                                                                                                                                                                                    |
| ------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                | `assets.id`, and `tree.id` (shared PK)                                   | new UUID; the same UUID is reused for this asset's `tree` row, exactly like `pages.id`/`tree.id`                                                                                                                                                                                                                     |
| `filename`          | `assets.fileName`, `tree.fileName`                                       | direct rename, written to both rows                                                                                                                                                                                                                                                                                  |
| `hash`              | **NO DESTINATION YET**                                                   | no `hash` column on 3.0 `assets` (3.0 `tree.hash` exists but is populated from content hashing at write time, not carried from the 2.x asset hash verbatim without confirming the two hash algorithms agree — flag for the importer feature, not assumed equal here)                                                 |
| `ext`               | `assets.fileExt`                                                         | direct rename                                                                                                                                                                                                                                                                                                        |
| `kind`              | `assets.kind`                                                            | **enum values differ**: 2.x is `enum('binary','image')` (two values); 3.0's `assetKind` enum is `('document','image','other')` (three values, no `binary`) — 2.x `'binary'` needs a value remap, most naturally to 3.0 `'other'`                                                                                     |
| `mime`              | `assets.mimeType`                                                        | direct rename                                                                                                                                                                                                                                                                                                        |
| `fileSize`          | `assets.fileSize`                                                        | **unit change**: 2.x stores this "in kilobytes" per its own column comment; 3.0's schema comment says "in bytes" — needs a ×1024 conversion, not a straight copy                                                                                                                                                     |
| `metadata`          | `assets.meta`                                                            | direct rename (json → jsonb)                                                                                                                                                                                                                                                                                         |
| `createdAt`         | `assets.createdAt`                                                       | direct                                                                                                                                                                                                                                                                                                               |
| `updatedAt`         | `assets.updatedAt`                                                       | direct                                                                                                                                                                                                                                                                                                               |
| `folderId`          | `tree.folderPath`                                                        | structural transform, not a column mapping: resolved by walking `assetFolders.parentId` (see the `assetFolders` section) into the ltree ancestor path for the paired `tree` row                                                                                                                                      |
| `authorId`          | `assets.authorId`                                                        | remapped through the users old-id → new-UUID table                                                                                                                                                                                                                                                                   |
| (none)              | `assets.isSystem`, `assets.data`, `assets.preview`, `assets.storageInfo` | **new in 3.0, no 2.x column source** — `data` is populated from the `assetData` row (see below), `preview` has no 2.x source at all (2.x never generated/stored a separate thumbnail blob for every asset the way 3.0 does), `storageInfo` is populated by the storage layer post-import, not by the importer itself |

## assetData

| 2.x `assetData` column | 3.0 destination | Transform / notes                                                                                         |
| ---------------------- | --------------- | --------------------------------------------------------------------------------------------------------- |
| `id`                   | `assets.id`     | shared PK with the `assets` row this blob belongs to — no separate row on the 3.0 side at all             |
| `data`                 | `assets.data`   | inlined directly onto the `assets` row (the whole point of point 3 above: the detail-table split is gone) |

## assetFolders

3.0 has no separate `assetFolders`/folder table at all — asset folder placement is expressed purely
as `tree` rows with `type = 'folder'`, sharing the same ltree hierarchy pages use (point 4 above).

| 2.x `assetFolders` column | 3.0 destination   | Transform / notes                                                                                                                                                                                                                                                                                      |
| ------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `id`                      | `tree.id`         | new UUID for a `tree` row with `type = 'folder'`                                                                                                                                                                                                                                                       |
| `name`                    | `tree.title`      | direct rename                                                                                                                                                                                                                                                                                          |
| `slug`                    | `tree.fileName`   | direct rename — the folder-segment name used in the ltree path                                                                                                                                                                                                                                         |
| `parentId`                | `tree.folderPath` | structural transform: the walked integer parent chain (a **second, independent** chain from `pageTree.parent` in 2.x) is folded into the **same** `tree.folderPath` ltree namespace pages use in 3.0 — 2.x kept page folders and asset folders as two unrelated hierarchies; 3.0 unifies them into one |

## tags

3.0's `tags` table is **not the source of truth any more** — it becomes a per-site usage-count cache,
rebuilt/maintained as pages are saved, with the actual list of a page's tags denormalized directly
onto `pages.tags` (a `text[]`). This is the collapse the task description calls out explicitly.

| 2.x `tags` column | 3.0 destination                        | Transform / notes                                                                                                                                                                    |
| ----------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `id`              | `tags.id`                              | new UUID, if the importer chooses to pre-seed the cache table rather than let it rebuild from `pages.tags` on first save                                                             |
| `tag`             | `tags.tag`, and `pages.tags[]` entries | the tag string itself is what actually needs to land — primarily as elements of the `pages.tags` array on every page that referenced it (via `pageTags`, see below)                  |
| `title`           | **NO DESTINATION YET**                 | no display-title-distinct-from-slug concept on 3.0's `tags` row (`tag` is the only string column besides the usage count)                                                            |
| `createdAt`       | `tags.createdAt`                       | direct, if pre-seeding the cache row                                                                                                                                                 |
| `updatedAt`       | `tags.updatedAt`                       | direct, if pre-seeding the cache row                                                                                                                                                 |
| (none)            | `tags.usageCount`, `tags.siteId`       | **new in 3.0, no 2.x source** — `usageCount` is a derived count (recomputable from how many `pages.tags` arrays contain this tag), `siteId` is the import-time constant from point 2 |

## pageTags

**Collapses entirely into `pages.tags`, no surviving relation table.** Each 2.x `pageTags` row (one
per page↔tag pairing) becomes one element appended to the target page's `pages.tags` text array — the
join table itself has no 3.0 counterpart because there is no longer a join to perform at read time.

| 2.x `pageTags` column | 3.0 destination                                            | Transform / notes                                                                                     |
| --------------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `id`                  | **NO DESTINATION YET**                                     | the join row itself has nothing to become — it collapses into an array element, not a row             |
| `pageId`              | selects which `pages.tags[]` the tag string is appended to | resolved through the pages old-id → new-UUID table                                                    |
| `tagId`               | the tag string appended to `pages.tags[]`                  | resolved through `tags.tag` (2.x `tagId` → the tag's string value, which is what 3.0 actually stores) |

## pageHistoryTags

Same collapse as `pageTags`, but the destination array lives on the `meta` blob of the corresponding
`pageHistory` row rather than on a top-level `pages.tags` column, per the `pageHistory` section above
(`tags` folds into `meta.tags`, mirroring `description`/`publishState`/etc.). Recall the naming trap
`2.5x-source-schema.md` flags: this table's `pageId` column targets `pageHistory.id`, **not**
`pages.id`.

| 2.x `pageHistoryTags` column | 3.0 destination                                                       | Transform / notes                                                                                   |
| ---------------------------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `id`                         | **NO DESTINATION YET**                                                | collapses into an array element inside `meta.tags`, not a row                                       |
| `pageId`                     | selects which `pageHistory.meta.tags[]` the tag string is appended to | targets `pageHistory.id` (not `pages.id`), resolved through the pageHistory old-id → new-UUID table |
| `tagId`                      | the tag string appended to `pageHistory.meta.tags[]`                  | resolved through `tags.tag`, same as `pageTags.tagId` above                                         |

## navigation

2.x's `navigation` is one singleton row (string-keyed, holding a whole JSON tree). 3.0's `navigation`
is a real per-site table with a UUID PK.

| 2.x `navigation` column | 3.0 destination                      | Transform / notes                                                                                                                                                                                                        |
| ----------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `key`                   | **NO DESTINATION YET**               | 2.x's singleton string key (always `'site'` in practice, per Feature 412's own description) has nothing to map to — 3.0's `navigation` has a real generated `id`, not a string key, and is looked up by `siteId` instead |
| `config`                | `navigation.items`                   | the JSON tree becomes the `items` jsonb column; the internal shape of the tree itself is not verified here (out of scope for a column-level mapping — a structural content transform, likely non-trivial)                |
| (none)                  | `navigation.id`, `navigation.siteId` | **new in 3.0, no 2.x source** — `id` is a fresh UUID, `siteId` is the import-time constant from point 2                                                                                                                  |

## settings

| 2.x `settings` column | 3.0 destination        | Transform / notes                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `key`                 | `settings.key`         | direct — both sides keep this table string-keyed (`settings.key varchar primaryKey()` on 3.0 too), so no UUID remap applies here at all                                                                                                                                                                                                                                                                           |
| `value`               | `settings.value`       | direct (json → jsonb) **key-by-key**, not verbatim: individual 2.x setting keys are not guaranteed to have the same name/shape as their 3.0 equivalent (parallel to the `authentication`/`storage` module-config caveat below) — auditing every 2.x setting key against 3.0's is out of this task's scope (no `base.yml`-vs-2.x-settings diff was performed) and should be a follow-up, not assumed complete here |
| `updatedAt`           | **NO DESTINATION YET** | 3.0's `settings` table has no `updatedAt` column at all                                                                                                                                                                                                                                                                                                                                                           |

## authentication

| 2.x `authentication` column | 3.0 destination                                                                                                                | Transform / notes                                                                                                                                                                                                                                                                        |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `key`                       | selects which `authModuleId` a user's `users.auth` entries use; is not itself stored as a string on 3.0's `authentication` row | 3.0's `authentication.id` is a UUID, not the module key; `authentication.module` (see below) holds what 2.x called `key`                                                                                                                                                                 |
| `isEnabled`                 | `authentication.isEnabled`                                                                                                     | direct                                                                                                                                                                                                                                                                                   |
| `config`                    | `authentication.config`                                                                                                        | **key-by-key remap required, module by module** — see [Module config remapping](#module-config-remapping); the `local` module happens to need none (its 2.x `props: {}` is empty), but this is not true of every module and must be spot-checked per module before an importer trusts it |
| `selfRegistration`          | `authentication.selfRegistration` AND `authentication.autoProvision`                                                           | 2.x carried one combined flag; 3.0 splits its target into `selfRegistration` (enforced only for a form-based module) and `autoProvision` (enforced only for a redirect-based one) — see WP #2130. The importer mirrors the 2.x value onto both columns, since the source has no way to say which of the two it meant and 3.0 only ever enforces the one that applies to the module the row lands on |
| `domainWhitelist`           | `authentication.allowedEmailRegex`                                                                                             | **structural transform, not a rename**: 2.x stores an explicit domain list (json array); 3.0 stores one regex string — a 2.x domain list needs compiling into an equivalent regex, not a straight copy                                                                                   |
| `autoEnrollGroups`          | `authentication.autoEnrollGroups`                                                                                              | direct shape (array), but element type changes from 2.x integer group ids to 3.0 `uuid[]` — remapped through the groups old-id → new-UUID table                                                                                                                                          |
| `order`                     | **NO DESTINATION YET**                                                                                                         | added `2.5.1.js` on the 2.x side (display/priority ordering); no `order` column on 3.0's `authentication` table                                                                                                                                                                          |
| `strategyKey`               | `authentication.module`                                                                                                        | direct rename — this is the module directory name (`local`, `google`, …), which 3.0 calls `module` where 2.x (post-`2.5.1.js`) called it `strategyKey` (distinct from the row's own `key`, which no longer survives as a column at all — see above)                                      |
| `displayName`               | `authentication.displayName`                                                                                                   | direct                                                                                                                                                                                                                                                                                   |
| (none)                      | `authentication.id`                                                                                                            | **new in 3.0, no 2.x source** — fresh UUID, which is also the value written into `users.auth`'s per-strategy map key                                                                                                                                                                     |

## storage

| 2.x `storage` column | 3.0 destination                                                                                       | Transform / notes                                                                                                                                                                                                                                                                                                                                     |
| -------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `key`                | `storage.module`                                                                                      | direct rename — the module directory name (`git`, `s3`, …)                                                                                                                                                                                                                                                                                            |
| `isEnabled`          | `storage.isEnabled`                                                                                   | direct                                                                                                                                                                                                                                                                                                                                                |
| `mode`               | `storage.syncMode`                                                                                    | direct copy, but only when the source value is one of the target module's own declared `supportedModes` (most modules only ever support `push`; `git` supports `sync`/`push`/`pull`) — an unsupported value is dropped and reported rather than written, see `migration/mappers/storage.ts`                                                          |
| `config`             | `storage.config`                                                                                      | **key-by-key remap required, module by module** — see [Module config remapping](#module-config-remapping) below; the `git` and `s3` modules were spot-checked and both need real prop-level changes, not a straight copy                                                                                                                              |
| `syncInterval`       | `storage.scheduleOverride`                                                                            | converted, not copied — 2.x stored this as a raw five-field cron expression, 3.0's `scheduleOverride` is an ISO-8601 duration; only the "every N minutes"/"every N hours" cron shapes convert losslessly, anything else (a pinned time of day, a day-of-week restriction, …) is dropped and reported, see `migration/mappers/storage.ts`               |
| `state`              | `storage.state`                                                                                       | shape changes: 2.x's `state` was module-defined free-form json; 3.0's schema comment pins it to `{ setup: 'notconfigured' \| 'pendinginstall' \| 'configured' }` specifically — a 2.x module's own state shape is not guaranteed to match this and needs a per-module check, same caveat as `config`                                                  |
| (none)               | `storage.id`, `storage.contentTypes`, `storage.assetDelivery`, `storage.versioning`, `storage.siteId` | **new in 3.0, no 2.x column source** — `id` is a fresh UUID; `contentTypes`/`assetDelivery`/`versioning` are new per-module toggle groups with no 2.x equivalent at all (2.x had no concept of selecting which content types sync, or of separately toggling streaming vs. direct-access delivery); `siteId` is the import-time constant from point 2 |

---

## Module config remapping

Per-module `config` blobs are not portable as-is: 2.x and 3.0 prop names for the _same_ module `key`
diverge, confirmed by diffing `definition.yml` for three modules directly.

### `local` authentication module

- 2.x: `server/modules/authentication/local/definition.yml` @ `requarks/wiki` `main` declares
  `props: {}` — **no configurable props at all**.
- 3.0: `backend/modules/authentication/local/definition.yml` declares three props: `enforceTfa`,
  `emailValidation`, `allowForgotPassword`.
- **Finding**: because 2.x's `config` for this module is always empty, there is nothing to remap
  key-by-key for `local` specifically — the three 3.0 props are new capabilities with no 2.x source
  and simply take their schema defaults on import. This module is the good case, not the general
  one; `git` and `s3` below show why the remap step cannot be skipped for every module just because
  it was a no-op here.

### `git` storage module

- 2.x: `server/modules/storage/git/definition.yml` @ `requarks/wiki` `main`.
- 3.0: `backend/modules/storage/git/definition.yml`.
- **Findings**:
  - `sshPrivateKeyMode`'s enum **values** were renamed, not just relabeled: 2.x uses
    `'path'`/`'contents'`; 3.0 uses `'path'`/`'inline'`. A 2.x `config.sshPrivateKeyMode: 'contents'`
    must be rewritten to `'inline'` on import, not copied verbatim, or the imported config silently
    fails validation against 3.0's `enum`.
  - `branch`'s default value changed from 2.x `'master'` to 3.0 `'main'` — only matters for a 2.x
    row that never had a `branch` set at all (relying on the default), since an explicit value
    copies straight across either way.
  - `alwaysNamespace` (2.x, boolean, "whether to put content from the primary language into a
    subfolder") has **no 3.0 prop with that name or an equivalent** in `backend/modules/storage/git/definition.yml`
    — **NO DESTINATION YET** for this specific config key, even though the module itself maps
    cleanly.
  - The remaining props (`authType`, `repoUrl`, `sshPrivateKeyPath`, `sshPrivateKeyContent`,
    `verifySSL`, `basicUsername`, `basicPassword`, `defaultEmail`, `defaultName`, `localRepoPath`,
    `gitBinaryPath`) keep identical names on both sides and copy straight across.

### `s3` storage module

- 2.x: `server/modules/storage/s3/definition.yml` @ `requarks/wiki` `main`.
- 3.0: `backend/modules/storage/s3/definition.yml`.
- **Findings** — this is the module with the largest real reshape:
  - 2.x has a single flat `region` string prop (free text, AWS-only — the module description itself
    says "Amazon S3", full stop). 3.0 splits this into a `mode` selector (`aws` / `do` /
    `custom`) plus **three** separate region-shaped props gated by `mode`: `awsRegion` (a
    constrained enum of real AWS region codes), `doRegion` (a DigitalOcean Spaces region enum), and
    `endpoint` (a free-text URI for `custom`). A 2.x row's `config.region` value maps to
    3.0 `config.awsRegion` **and** requires synthesizing `config.mode: 'aws'` explicitly, since 2.x
    never recorded a mode (2.x S3 support was implicitly AWS-only) and 3.0 has no default that
    infers it.
  - `bucket`, `accessKeyId`, `secretAccessKey` keep identical names and copy straight across.
  - `storageTier`, `sslEnabled`, `s3ForcePathStyle`, `s3BucketEndpoint` are **new in 3.0, no 2.x
    source** — none of these existed as configurable props in the 2.x module at all, so they take
    their `definition.yml` defaults (or are left unset, for the ones without one) on import.

---

## Summary: NO DESTINATION YET rows, by cause

Grouped here for anyone scoping the follow-up features (414/416/418/420) that will need to either
build the missing destination or explicitly decide to drop the field:

- **A whole 3.0 feature doesn't exist yet**: `comments` (all columns — Epic 335), `pageLinks` (all
  columns — no link-index feature), `navigation.key` (no singleton-key concept).
- **A 3.0 column was deliberately dropped as a product decision**, not merely unbuilt:
  `pageHistory.render` (documented in-schema as intentionally not stored), `pages.isPrivate` /
  `pages.privateNS` / `pageTree.isPrivate` / `pageTree.privateNS` (superseded by page-rule
  permissions), `pageTree.depth` / `pageTree.ancestors` (superseded by `ltree` operators),
  `storage.mode` (2.x's own value was likely a copy-paste bug per `2.5x-source-schema.md` and 3.0
  never modeled sync direction), `storage.syncInterval` (sync scheduling "not modelled yet" per the
  `git` module's own `definition.yml` comment).
- **Needs a follow-up spot-check, not yet audited here**: `pages.extra`, `pages.createdAt` (folds
  into a synthesized `pageHistory` row instead), `pageHistory.createdAt`, `pageHistory.hash`,
  `users.pictureUrl`, `users.localeCode`, `users.defaultEditor`, `tags.title`,
  `authentication.order`, every individual `settings.value` key (a full 2.x-setting-key vs.
  `base.yml`-key audit is out of this task's scope), and any `authentication`/`storage` module
  other than the three spot-checked above (`local`, `git`, `s3`) — each additional module (`google`,
  `github`, `oidc`, `disk`, `db`, `azure`, `gcs`, `sftp`) needs the same `definition.yml` diff before
  its `config` blob can be trusted to remap correctly.
