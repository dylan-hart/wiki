# Cobalt mockup-diff sign-off log

Consolidated record for OpenProject #2784 (child of Feature #2763, "Cobalt: verification & QA").
This doc does not perform any diffing itself — it consolidates the results already recorded by the
Tasks that did: `#2772`/`#2773` (Primitives light/dark), `#2774`-`#2780` (the seven screen-diff
Tasks) and `#2781` (the unmocked-admin-pages token-inheritance spot-check). Every row cites the
OpenProject comment id the finding was taken from, so a claim here can be traced back to its source
rather than taken on faith.

Row order and grouping follows `ui-redesign-cobalt/HANDOFF.md`'s "Screens in this folder" table: the
16 light screen pairs (rows 1-16), plus the 2 dedicated Cobalt-dark mockups (Page View Dark,
Primitives Dark — rows 17-18). The handoff's 17th table row, "Aesthetic setting", is a separate,
18th screen with its own mockup that is not one of these 18 rows — see
[Known gap](#known-gap-the-aesthetic-setting-screen-was-never-diffed).

**Resolution categories** used in the table, per `HANDOFF.md`'s own tie-break rules plus the two
additional outcomes this round actually produced:

- **page-view-wins** — the Cobalt Page View mockup (#2774) is hand-authored and authoritative; a
  derived screen that disagreed with it on a shared primitive follows the page view.
- **doc-wins** — a derived screen disagreed with `HANDOFF.md`'s own written guidance; the doc wins.
  No case of this arose in this round (recorded here for completeness, not padding).
- **genuine defect — fixed** — an actual implementation gap (a literal color bypassing the token
  layer, a wrong `color` prop, a missing shape-token class) found and fixed in place.
- **deferred to Feature #2763 (frozen-primitive gap)** — a real disagreement whose fix requires
  touching a shared primitive or token file outside the diffing task's own file ownership for this
  round. Logged with an owner and a next step, not silently dropped — see
  [Frozen-primitive gaps](#frozen-primitive-gaps-deferred-to-feature-2763) below.
- **not diffed — no dedicated task this round** — the screen has a mockup, but no Task in this
  epic's `blockedBy` set ever ran a rendered-vs-mockup comparison against it. Used once, for row 10
  (Profile); see that row and the [Known gap](#known-gap-the-aesthetic-setting-screen-was-never-diffed)
  section for the parallel case this doc found outside the 18 required rows.

## The 18 rows

| #   | Screen                             | Mockup diff performed                                                                                                                                                                                                                                                                                    | Disagreements & resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `cobalt + dark`                                                                                                                                                                                                       |
| --- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Page view (canonical)              | Yes — Task #2774, 2026-09-07 (comment 7512)                                                                                                                                                                                                                                                              | 9 fixed (genuine defect — token wiring: page-header banner, actions rail, nav/header/footer/tags/sidebar-footer-button tokens). 5 logged, not fixed: the Contents/Tags/Revision rail as 3 separate cards (real layout restructuring, not a token swap — deferred, no single owner yet), the rail's icon count (mockup shows fewer than the app's permission-driven real count — not a defect, a functional difference), EditorMarkdown's own untouched chrome (large file, no illegible text, not yet Cobalt-styled), the code block's exact dark-mode hex (needs a new `--color-dark-6`-family value), the "bash" language label (a render-pipeline feature, not a style fix). `PageTags.vue` collision with #2777: **page-view-wins**, #2774 is primary owner.                                                                                                                                                                                                                               | Diffed directly against the dedicated `Page View Dark 3x - Cobalt` mockup — see row 17.                                                                                                                               |
| 2   | Editor                             | Yes — Task #2774, 2026-09-07 (comment 7512), diffed against `Editor 3x - Cobalt` alongside the page view                                                                                                                                                                                                 | Shares the page view's gradient-header/rail/code-block fixes. Code-block preview styling (`#10194a`, 8px radius, `bash` label, syntax-token contrast) re-checked and correct.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | No dedicated dark mockup. **Correct by token inheritance** (comment 7512, criterion 2 — "Editor's dark render is correct by token inheritance").                                                                      |
| 3   | History                            | Yes — Task #2776, 2026-09-07 (comment 7511)                                                                                                                                                                                                                                                              | 2 fixed (genuine defect): `<style lang="scss">` block read compile-time SCSS constants instead of `var(--color-*)`, so no Cobalt color applied at all; the "Current" badge/A-B letter plates used `color="primary"` where the mockup shows the white-text accent role — swapped to `color="accent"`. 1 deferred: `--color-accent-dark` has no Cobalt-light override (root value leaks through). 1 deferred: `.card-header`'s Sass-constant title band (shared by every dialog app-wide, primitive-layer item).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No dedicated dark mockup. **Correct by token inheritance** once the `var()` swap landed (comment 7511).                                                                                                               |
| 4   | File manager                       | Yes — Task #2776, 2026-09-07 (comment 7511), same pass as History                                                                                                                                                                                                                                        | 1 fixed (genuine defect): Insert button's `color="primary"`/`$primary` swapped to the white-text accent role, same root cause as History's badge fix. 1 deferred: details-pane background (`#fbfcfe` Ledger vs `#ffffff` mockup) has no matching token yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | No dedicated dark mockup. **Correct by token inheritance** (comment 7511).                                                                                                                                            |
| 5   | Graph                              | Yes — Task #2777, 2026-09-07 (comment 7508)                                                                                                                                                                                                                                                              | 1 fixed (genuine defect): panel/card treatment had no `--radius-card`/`--shadow-card` consumption at all — added. Hardcoded SCSS literals (not `var()`) swapped to tokens, same fix class as Tags/Search below. `GraphClientTypeFilter` checked, no gap found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No dedicated dark mockup. **Correct by token inheritance** after the literal-to-`var()` swap (comment 7508) — previously would not have followed dark at all; now does.                                               |
| 6   | Inbox / Inbox review               | Yes — Task #2778, 2026-09-07 (comment 7510)                                                                                                                                                                                                                                                              | 3 fixed (genuine defect): overlay background/text read Ledger-only SCSS literals instead of `var(--color-*)`; sidebar rail had no Cobalt token wiring (background/hairline/row-text/icon/active-row all added); Decline button's border used the wrong accent literal. 5 deferred/logged: `.card-header` title band (same primitive-layer item as History), `WDialog`'s `--radius-dialog` gap (same as #2772's flag), `WAvatar square`'s corner radius (shared-primitive), a "slate" avatar-plate fill with no assigned token, the Monaco diff editor's non-tokenized theme (deliberate, pre-existing) and `.inbox-review-count`'s chip text color (no dark mockup to confirm against, logged rather than guessed).                                                                                                                                                                                                                                                                            | No dedicated dark mockup. **Correct by token inheritance** — the fixed tokens are aesthetic-scoped, not light/dark-scoped, so they hold in `cobalt + dark` automatically (comment 7510, acceptance #2).               |
| 7   | Tags                               | Yes — Task #2777, 2026-09-07 (comment 7508)                                                                                                                                                                                                                                                              | 2 fixed (genuine defect): hardcoded SCSS literals swapped to `var(--color-*)` (previously zero Cobalt color applied); `color="primary"` on the "current selection" chip swapped to `color="accent"` plus `--shadow-primary`, matching the mockup's red glow (a real hue bug, invisible under Ledger only because Ledger's primary/accent share one value). 2 deferred: the default `WChip` outline vs. the mockup's `shadow-card` plate (shared-primitive shape-variant gap); `--color-slate`'s fixed value for "available tag" chips doesn't track Cobalt navy (no clean token maps to the role). `PageTags.vue` collision: **page-view-wins**, #2777 did not touch it (deferred to #2774 per the coordination note).                                                                                                                                                                                                                                                                         | No dedicated dark mockup. **Correct by token inheritance** after the `var()` swap (comment 7508) — previously rendered pure Ledger-dark; now correctly falls back to the dark token block.                            |
| 8   | Search                             | Yes — Task #2777, 2026-09-07 (comment 7508), same pass as Tags/Graph                                                                                                                                                                                                                                     | 2 fixed (genuine defect): same literal-to-`var()` class of fix; active-sort-icon `color="primary"` swapped to `color="accent"`, same hue-bug class as Tags' chip. 1 deferred: default `WChip` outline vs. mockup's `shadow-card` plate (same shared-primitive gap as Tags).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | No dedicated dark mockup. **Correct by token inheritance** (comment 7508).                                                                                                                                            |
| 9   | Login / Auth screens               | Yes — Task #2779, 2026-09-07 (comment 7509)                                                                                                                                                                                                                                                              | 5 fixed (genuine defect): both files' `<style>` blocks converted from literal SCSS vars to `var(--color-*)`; `.auth-marks`'s corner-mark pseudo-elements gated on `display: var(--corner-marks)`; `--shadow-primary` wired onto every primary CTA (previously unconsumed anywhere in the codebase); every CTA's `color="primary"` swapped to `color="accent"` (the white-text-carrying role — a real hue bug, same root cause as Tags/Search); coordinated with Feature #2747's Tasks #2750/#2751 login-animation work on the same files with no conflict (acceptance #4). 1 deferred: `chromeColor`'s secondary-button tone has no dedicated Cobalt token (same already-logged gap as `NavEditMenu.vue`/`NavEditOverlay.vue`, flagged rather than a third ad-hoc guess).                                                                                                                                                                                                                      | No dedicated dark mockup. **Correct by token inheritance** — verified against the dark token block alone per comment 7509's risk note (d).                                                                            |
| 10  | Profile                            | **No** — no dedicated diff task this round. Feature #2756's only Task, #2770, added the `site \| ledger \| cobalt` toggle control to `ProfileInfo.vue`/`UserEditOverlay.vue` (comment 7481); its scope was explicitly "pure UI mirror of the already-shipped `appearance` control", never a mockup diff. | Not evaluated — see [Known gap](#known-gap-the-aesthetic-setting-screen-was-never-diffed) for the parallel case and the recommended follow-up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Not spot-checked. **Flagged as a gap** for follow-up under Feature #2763 (or a new Task under #2756).                                                                                                                 |
| 11  | Admin dashboard / General / Blocks | Yes — Task #2780, 2026-09-07 (comment 7513), all three screens in one pass                                                                                                                                                                                                                               | 1 fixed (genuine defect): `AdminLayout.vue`'s `<style>` predated the token architecture entirely (bare SCSS constants/literal `#fff`) — header, sidebar (including its own distinct, non-inheriting token set per the coordination note), area label, page eyebrow and count-badge all wired to `body.body--cobalt`/`body.body--cobalt.body--dark`. `AdminDashboard.vue`/`AdminGeneral.vue`/`AdminBlocks.vue` needed zero changes (already token-driven). 4 deferred/logged: header's secondary chrome (Exit/EN/avatar) kept as-is (not in Owns); the mockup's "beta" badge deliberately not restored (pre-existing, tested divergence, OpenProject #2635); the page-icon plate's exact treatment (ambiguous token choice, not guessed); `AdminBlocks.vue`'s tab/credential-id chip radius (`WChip` is a frozen shared primitive, swapping component types risked a regression). 1 pre-existing, unrelated test failure confirmed via `git stash` to predate this task's changes, not touched. | No dedicated dark mockup. Admin sidebar has its own distinct (non-inheriting) Cobalt token set, checked separately per the coordination note — **confirmed correct**, sidebar included (comment 7513, acceptance #2). |
| 12  | Page properties                    | Yes — Task #2775, 2026-09-07 (comment 7507), one of 4 screens in this task                                                                                                                                                                                                                               | 1 fixed (genuine defect): the Relations section's position indicator used `<w-chip>` (pill) where the radii-sweep role assignment calls for `<w-badge>` (mark) — swapped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | No dedicated dark mockup. **Correct by token inheritance** — component already composed token-aware shared primitives (comment 7507).                                                                                 |
| 13  | Table editor                       | Yes — Task #2775, 2026-09-07 (comment 7507)                                                                                                                                                                                                                                                              | 0 fixed. 1 deferred: the grid's uniform hairline-bordered cells vs. the mockup's banded/unbanded distinction conflicts with `border-collapse: collapse`'s layout — logged rather than guessed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No dedicated dark mockup. **Correct by token inheritance** (comment 7507, acceptance #2).                                                                                                                             |
| 14  | Block picker                       | Yes — Task #2775, 2026-09-07 (comment 7507)                                                                                                                                                                                                                                                              | 1 fixed (genuine defect): the unselected block card and its icon plate had zero shape-token consumption — added `rounded-card`/`shadow-card` additively (no Ledger visual change, its own tokens are 0/none). 1 deferred: `UpOneLevelBtn.vue` (shared, not in this task's Owns, also used elsewhere) has no at-rest plate the mockup wants.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | No dedicated dark mockup. **Correct by token inheritance** (comment 7507).                                                                                                                                            |
| 15  | Menus                              | Yes — Task #2775, 2026-09-07 (comment 7507)                                                                                                                                                                                                                                                              | 1 fixed (genuine defect): `PageNewMenu.vue` was missing the mockup's "Beta" indicator beside the experimental WYSIWYG row — added, gated on the existing `flagsStore.experimental` flag. 2 deferred: `WMenu.vue` has no generic "mark the trigger row while open" hook the sidebar context menu wants (shared-primitive item); `TreeBrowserDialog.vue`'s selection highlight resolves through a Ledger-only, compile-time SCSS variable with no Cobalt-aware equivalent yet (architectural, out of ownership).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | No dedicated dark mockup. **Correct by token inheritance** (comment 7507).                                                                                                                                            |
| 16  | Primitives                         | Yes — Task #2772, 2026-09-07 (comment 7492)                                                                                                                                                                                                                                                              | 8 fixed (genuine defect): none of the shared `W*` primitives consumed the landed shape tokens at all (every one hardcoded a fixed radius regardless of aesthetic) — `WBtn`/`WCard`/`WChip`/`WBadge`/`WCheckbox`/`WToggle`/`WBtnToggle`/`WInput`/`WSelect`/`WBanner`/`WNotifications` all swapped to the matching `rounded-*`/`shadow-card` utility. SCSS-literal grep across `components/shared/` and `notify.js` came back clean. 4 deferred to Feature #2763 — see [Frozen-primitive gaps](#frozen-primitive-gaps-deferred-to-feature-2763).                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Diffed directly against the dedicated `Primitives Dark 3x - Cobalt` mockup — see row 18.                                                                                                                              |
| 17  | Page View Dark                     | Yes — Task #2774, 2026-09-07 (comment 7512), diffed against `Page View Dark 3x - Cobalt`, one of the epic's two hand-authored dark mockups                                                                                                                                                               | 0 disagreements — page view's dark render matches the mockup exactly (comment 7512, acceptance criterion 2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | N/A — this row is itself the dedicated dark diff for row 1.                                                                                                                                                           |
| 18  | Primitives Dark                    | Yes — Task #2773, 2026-09-07 (comment 7502), diffed against `Primitives Dark 3x - Cobalt`, the other hand-authored dark mockup                                                                                                                                                                           | 0 new fixes needed — the large majority already renders correctly purely through shape-token inheritance from #2771/#2772 (verified component-by-component: WBtn, WBadge, WChip, WCheckbox, WToggle, WBtnToggle, WCard, WCardHeader, WFieldFrame, WInput/WSelect, the loading/progress family, WBanner, WNotifications). 4 new dark-specific disagreements deferred to Feature #2763 (`.card-header`'s Sass-constant band, `WCardHeader`'s section-band color mismatch, `WInput`'s read-only-field dark background, `WFieldFrame`'s error-ring dark color) — see [Frozen-primitive gaps](#frozen-primitive-gaps-deferred-to-feature-2763). All 4 of #2772's light-mode flags re-verified still accurate for dark, carried forward rather than re-resolved differently.                                                                                                                                                                                                                         | N/A — this row is itself the dedicated dark diff for row 16.                                                                                                                                                          |

Beyond the 18 rows above, Task #2781 additionally spot-checked 6 unmocked admin settings pages
(`AdminSecurity`, `AdminGroups`, `AdminAuth`, `AdminScheduler`, `AdminAuditLog`, `AdminNavigation`)
for token inheritance, with no page-specific fix needed (comment 7521). This satisfies
`HANDOFF.md`'s "every other screen... spot-checked against the token block" requirement for the
admin surface specifically — it is not one of the 18 required rows itself, since those pages have no
mockup at all.

## Known gap: the Aesthetic setting screen was never diffed

`HANDOFF.md`'s screens table lists a mockup for **Aesthetic setting**
(`Cardinal Wiki - Aesthetic Setting 3x.dc.html`, options 1a/1b) beyond the 18 rows this WP's scope
covers. It was never diffed against its mockup by any Task in this epic: Feature #2755's only Task,
#2769, added the aesthetic toggle row to `AdminTheme.vue`'s Appearance card (comment 7499). Its own
scope was rendering the control and wiring `resetColors()` — not a mockup diff, and its acceptance
criteria never asked for one.

This mirrors row 10 (Profile) above exactly, for the same reason: neither Feature #2755 nor Feature
#2756 was ever in this Feature's `blockedBy` set (only #2757-2762's Tasks were), so no Task in this
round was positioned to catch either gap. Both surfaced only while assembling this consolidated log
against `HANDOFF.md`'s full screens table, not from any single screen-diff Task's own acceptance
criteria.

**Admin half — fixed, OpenProject #2809, 2026-09-07 (comment 7584).** `AdminTheme.vue`'s Appearance
card (`WSettingsCard` + `WSettingsRow` + `BlueprintIcon`) diffed against options 1a (Ledger) / 1b
(Cobalt). 1 fixed (genuine defect): every color row's plate icon was `tabler:color-swatch` (a
paint-tube/ribbon glyph) where both mockup options draw a palette-circle glyph — swapped to
`tabler:palette`, already used elsewhere in this codebase. Everything else checked either matches or
is already covered by a completed sibling fix: the Aesthetic/Dark-mode-row toggle and the color
rows' `WBtn`/`WToggle` rounding render correctly via token inheritance (#2772/#2773). 1 new deferral
to Feature #2763: `BlueprintIcon.vue` (the settings-row icon plate, shared across ~35 admin/profile
pages) has zero Cobalt-specific styling — it stays Ledger's square hairline plate under both
aesthetics, where the Cobalt mockup draws a filled, 6px-rounded, indigo-tinted plate with a
blue-accent glyph — a real visual disagreement, but a frozen-primitive-scale change outside a
single-screen diffing task's file ownership (added to
[Frozen-primitive gaps](#frozen-primitive-gaps-deferred-to-feature-2763) below). 2 reviewed and left
as-is, not fixed: the Dark mode and Primary color rows' hint copy reads differently from the
mockup's illustrative prose (the shipped copy correctly carries functional/accessibility information
the mockup's abbreviated text omits, and no row in the 18-row table above ever rewrote hint wording
to match mockup copy — that table's "genuine defect" category is explicitly
token/prop/icon, not wording); and the hex-value caption next to each swatch uses the fixed neutral
`text-grey-6` rather than an aesthetic-tracking blue-grey, which is the same shared "muted caption"
convention four other components already use identically, so changing it here alone would be
inconsistent rather than a fix. 2 logged as non-defects: the real screen renders 5 color rows where
the mockup only illustrates one ("Primary color") — the same "mockup shows fewer than the real,
data-driven count" pattern already established for row 1's rail icon count; and the mockup's Ledger
corner marks around the card are not reproduced by `WSettingsCard` (shared, ~35 pages, no corner
marks anywhere), consistent with every other settings-card mockup diff in the 18 rows never flagging
this as missing.

**Profile half — still open**, scoped to OpenProject #2810 (`ProfileInfo.vue` + `UserEditOverlay.vue`,
the mockup's second, "Profile › Identity › Preferences" card) per the epic coordination note; not
touched by #2809.

## Frozen-primitive gaps deferred to Feature #2763

Carried forward unchanged from #2772/#2773's own logging, per the epic coordination note's explicit
instruction that none of the 7 parallel screen-diff Tasks should independently "fix" a frozen shared
primitive:

- `WCard.vue`'s border reads `border-hairline`/`dark:border-hairline-dark` rather than a
  `--border-card` token (no dark-mode-specific value exists for one yet).
- `WDialog.vue` hardcodes Tailwind's `rounded-lg` instead of `--radius-dialog`, and already
  contradicts Ledger's own `--radius-dialog: 0` — a fix here is app-wide, not Cobalt-only.
- `--shadow-primary` needs an app-level decision ("which `color` prop value means the page's own
  primary action") that no single component can make on its own.
- `--q-info`/`-negative`/`-positive`/`-warning` are un-seeded by `aestheticDefaults.js`, so
  toast/banner fill colors stay Ledger-colored under Cobalt until that lands.
- `WConfirmDialog.vue`'s `.card-header` title band (`_base.scss`) draws from a compile-time Sass
  constant shared by 60+ non-shared dialogs app-wide, not the runtime CSS custom property system.
- `WCardHeader.vue`'s `.w-section-header` dark color and `WInput.vue`'s read-only dark background
  both want a ramp rung (`#0e1540`) that neither currently has.
- `WFieldFrame.vue`'s error-ring dark color lightens per the generic pre-Cobalt convention, but the
  mockup wants the same bright value light uses.
- `BlueprintIcon.vue`'s settings-row plate (shared across ~35 admin/profile pages) has no
  Cobalt-specific styling at all — square hairline plate, `--color-paper` fill, `--color-slate` glyph
  under both aesthetics, where the Cobalt mockup wants a filled, radiused, indigo-tinted plate with a
  blue-accent glyph. Found by OpenProject #2809 diffing `AdminTheme.vue`'s Appearance card (comment
  7584); out of that task's single-file ownership to fix.

None of these were fixed by this WP or by any of the 7 screen-diff Tasks — they are Feature #2763's
to resolve, tracked here so the deferral is visible in the consolidated log rather than only buried
in individual task comments.
