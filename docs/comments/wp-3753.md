# Comment recommendations: WP #3753

## backend/api/aiAssist.ts: `routes` (both route registrations)

- **Add** (above the options object of both `app.get('/sites/:siteId/ai/status', …)` and
  `app.post('/sites/:siteId/ai/generate', …)`):

  ```ts
  /*
    No route-level permissions: `write:pages` is a page rule permission, which `config.permissions`
    cannot express. `helpers/aiAssist.ts#evaluateAiAssist` checks it in the handler.
  */
  ```

- **Reason:** root `CLAUDE.md` (Permissions: "say so with a `No route-level permissions:`
  comment") requires this marker on every route that enforces a page-scoped permission in its
  handler. Without it, a reader sees a write-adjacent route with no `permissions` and takes it for an
  unguarded one.

## backend/helpers/aiAssist.ts: `aiRegistry`

- **Add** (directly above `export function aiRegistry()`):

  ```ts
  // TODO: read `CARDINAL.models.ai` directly once `models/ai.ts` is on this branch's base; the
  // structural cast exists only because the registry was not there when this was written.
  ```

- **Reason:** this is a genuine TODO. Once `models/ai.ts` exists, the `as unknown as` cast outlives
  its purpose and hides any type drift between this caller and `models/ai.ts#generate`.
