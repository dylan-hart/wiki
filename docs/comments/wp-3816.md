# WP 3816 comment recommendations

## backend/models/userCredentials.ts, `countAlternativeLogins`

**Add** (a line at the end of the existing JSDoc):

```
 * FIXME: a local entry counts whenever it is unrestricted, including the random password
 * `users.createUser()` writes for an account a provider auto-provisioned. That password is no way in
 * that the account holder knows, so `unlinkStrategy()` can remove such an account's only provider.
```

Reason: this is a genuine defect found while reading. The disconnect guard (`assertUnlinkable`) relies on this count, and nothing in the code shows that the counted password may be unknowable.

## backend/models/userCredentials.ts, `UserCredentials.unlinkStrategy`

**Add** (JSDoc above the method):

```
/**
 * `patchStrategyAuth()` can only merge into an entry, so this is its sibling for deleting one, under
 * the same {@link authLockKey} lock. The not-linked and last-way-in checks run inside the lock: two
 * concurrent removals of an account's only two providers would each see the other as a way in.
 *
 * @throws `ERR_UNLINK_LOCAL_STRATEGY`, `ERR_INVALID_USER`, `ERR_UNLINK_NOT_LINKED` or
 *         `ERR_UNLINK_LAST_LOGIN_METHOD`
 */
```

Reason: a reader cannot re-derive from the code why the guard re-runs under the lock instead of before it, and sibling methods document their `@throws` codes the same way.
