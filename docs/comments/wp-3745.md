# WP 3745 comment recommendations

Each entry is a real "why" not derivable from the code; all are Add, in the file's existing syntax.

## frontend/src/pages/Search.vue, `state.assets`

Add:

```js
/** A separate list from the page results: own endpoint and paging, and neither saved filter rows nor `orderBy` apply. `kind` outlives a query so a chosen type keeps narrowing the next search. */
```

Reason: explains why assets are not folded into `state.results` and why `resetAssets` leaves `kind` alone.

## frontend/src/pages/Search.vue, `showAssetsSection`

Add:

```js
/** A kind that narrows to nothing keeps the section up: hiding it would strand the reader on that kind. */
```

Reason: the `kind !== 'all'` clause looks redundant against the results-length check without it.

## frontend/src/pages/Search.vue, `performAssetSearch`

Add (at the `requestId !== assetRequestId` early return):

```js
// -> A newer search or kind change started while this one was in flight; its answer wins.
```

Reason: load-bearing stale-response guard, easy to strip as noise.

## frontend/src/pages/Search.vue, `performSearch` (before the page `runSearchRequest` call)

Add:

```js
// -> Asset text is matched against the words alone: tags and filter rows describe pages.
```

Reason: explains why `q` is used rather than the tags/filters the page request sends.

## frontend/src/pages/Search.vue, `performSearch` semantic branch

Add:

```js
// -> The semantic route reads pages only; no asset search runs in Semantic mode.
```

Reason: the `resetAssets()` call there is otherwise unexplained.
