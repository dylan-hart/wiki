# WP 3740 comment recommendations

## frontend/src/helpers/pendingImages.js, `resolvePendingImages`

The doc comment and the inline `// -> token is unique per call` comment were moved verbatim from `EditorMarkdown.vue` together with the function, not edited.

- **Change** the JSDoc. It describes only the fetch path and says nothing of `keepReferences`. Proposed text: "Async other half of `htmlToMarkdown`, which hands back `![alt](pending-image:N)` placeholders. `fetch` turns `data:`, `blob:` and `http(s):` sources into bytes; an unretrievable `src` (CORS, a `blob:` from a closed tab) drops just that image. `keepReferences` leaves the original `src` in place instead of fetching, for callers that cannot upload assets." Reason: the new mode is otherwise undocumented.

## frontend/src/helpers/pendingImages.js, `findUnresolvedImageReferences`

- **Add**: "Scans prose only: fenced and inline code are stripped first so documentation showing image syntax is not counted. `data:`, http(s) and root-absolute sources count as resolvable." Reason: the counting rules are not obvious from the regexes.
