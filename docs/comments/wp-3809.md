# WP 3809 comment recommendations

## backend/helpers/metrics.ts, `formatPrometheusMetrics`

**Change** (JSDoc above the function).

Current:

```
/**
 * Prometheus text exposition format (version 0.0.4). Hand-rolled rather than pulled in via
 * `prom-client`: every series is a gauge computed elsewhere, with no counters, histograms or
 * per-request registry to justify a client library's bookkeeping.
 */
```

Proposed:

```
/**
 * Prometheus text exposition format (version 0.0.4), hand-rolled rather than via `prom-client`: the
 * series are few and fixed, and the only per-event state (`helpers/pubsub.ts#notifierStats`) is a
 * handful of plain numbers per notifier, too little to justify a client library's registry.
 */
```

Reason: `formatPubsubMetrics` now emits counters and a histogram, so "every series is a gauge" and
"no counters, histograms" are false. The tradeoff (no `prom-client`) is still real and stays.

## backend/controllers/metrics.ts, file header above `routes`

**Remove** the first paragraph only. Keep the "Deliberately not under `/_api`" and "Outside `/_api`"
paragraphs as they are.

Current:

```
 * Prometheus scrape endpoint. Every series is a plain gauge already computed elsewhere, so the
 * exposition writer is hand-rolled in `helpers/metrics.ts` rather than pulling in `prom-client` —
 * there are no counters, histograms or registries to justify a client library.
```

Reason: the paragraph is now false because the scrape includes the pubsub counters and histogram.
What remains true (why there is no `prom-client`) repeats the explanation at
`helpers/metrics.ts#formatPrometheusMetrics`. `docs/operations.md`'s Metrics section points at
"the header comment in `backend/controllers/metrics.ts`" for the reason `/metrics` sits outside
`/_api`, which the kept paragraphs still carry.
