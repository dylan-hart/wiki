# Comment recommendations: WP 3723

## frontend/src/components/PageNewMenu.test.js, "draws the full-size plate on every create-menu row, and no target-folder line"

**Change.**

Current: `// -> One per row: markdown, code, asciidoc, redirect, import, batch import, upload, new folder`

Proposed: `// -> One per row, not one per editor`

Reason: the row list went stale when the "From template" row was added (the count assertion below it is now 9); a list of rows restates the template and rots on the next row.

## frontend/src/components/PageNewMenu.test.js, "drops every plate to the compact one at the pointer, and trims the batch import row"

**Change.**

Current: `// -> The same eight rows less the batch import, which the pointer-anchored menu does not carry`

Proposed: `// -> The same rows less the batch import, which the pointer-anchored menu does not carry`

Reason: "eight" is a fragile count that went stale when the "From template" row was added (now nine rows, eight here).
