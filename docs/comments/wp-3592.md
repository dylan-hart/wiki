# WP #3592 — recommended comment changes

Code comments were not added in this change. Suggested additions, per the rubric:

## `backend/tasks/simple/auto-tag-page.ts`

Above `task()`:

```ts
// -> Main-thread job: worker threads have no `CARDINAL.models.tags`/`search`, which applying tags needs.
//    Additive only: existing page tags are kept and no tag is ever created. `autoTagPending` is cleared
//    on every outcome except `ChunksNotEmbeddedError`, which must leave it set so the scheduler's retry
//    still finds the page.
```

Above the `pageEmbeddingChunks` query:

```ts
// -> `pageEmbeddingChunks` is not in `db/schema.ts` (see `tasks/workers/embed-page.ts`), hence raw SQL.
```

Above the `ChunksNotEmbeddedError` throw:

```ts
// -> A page with no `searchContent` never gets chunks, so it is matched on its title alone instead of retried.
```

## `backend/db/schema.ts` (`pages.autoTagPending`)

```ts
// -> Set when a page is created without tags; `tasks/simple/auto-tag-page.ts` clears it once it has run.
```
