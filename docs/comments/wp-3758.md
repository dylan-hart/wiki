# Comment recommendations — WP #3758

## `backend/api/search.ts` — `routes` (the JSDoc above the plugin function)

**Change.**

Current:

```ts
/**
 * The general search settings (`dictOverrides`, `semanticEnabled`) hold no secret, so they take
 * `manage:sites` like the rest of a site's settings. An engine's config can hold credentials, so
```

Proposed:

```ts
/**
 * The general search settings hold no secret, so they take `manage:sites` like the rest of a
 * site's settings. An engine's config can hold credentials, so
```

Reason: the list in brackets names the settings, and it went out of date when `autoTagThreshold`
and `autoTagMaxTags` were added. The PATCH body schema already lists them.

## `frontend/src/pages/AdminSearch.vue` — `saveSemanticEnabled`

**Change.**

Current:

```js
/**
 * On failure, re-fetches the semantic setting rather than leaving the toggle showing what the reader
 * clicked: a stale `state.semanticAvailable` lets a click through that the server then refuses.
 */
```

Proposed:

```js
/**
 * On failure, re-fetches the card's settings rather than leaving the controls showing what the
 * reader entered: a stale `state.semanticAvailable` lets a click through that the server then
 * refuses.
 */
```

Reason: the comment is out of date. The re-fetch now restores the auto-tag threshold and max-tags
fields as well as the toggle (`applySemantic`).
