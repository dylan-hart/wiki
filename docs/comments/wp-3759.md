# WP 3759 comment recommendations

## backend/api/search.ts, `routes` (file-level JSDoc)

**Change**

Current:

```
 * The general search settings (`dictOverrides`, `semanticEnabled`) hold no secret, so they take
```

Proposed:

```
 * The general search settings (`dictOverrides`, `semanticEnabled`, `semanticMinMatch`) hold no
 * secret, so they take
```

Reason: correct: the list no longer names every setting the `manage:sites` PATCH route accepts.

## backend/models/search.ts, `SearchConfig`

**Change**

Current:

```
/**
 * The per-site search settings that belong to no engine. `dictOverrides` is a free-form locale ->
 * dictionary map rather than a scalar prop; `semanticEnabled` is engine-independent (semantic search
 * is always backed by Postgres/pgvector) and is ANDed with the instance-wide
 * `CARDINAL.capabilities.semanticSearch` before the feature is reachable.
 */
```

Proposed:

```
/**
 * The per-site search settings that belong to no engine. `dictOverrides` is a free-form locale ->
 * dictionary map rather than a scalar prop; `semanticEnabled` is engine-independent (semantic search
 * is always backed by Postgres/pgvector) and is ANDed with the instance-wide
 * `CARDINAL.capabilities.semanticSearch` before the feature is reachable. `semanticMinMatch` is a
 * 0-100 percentage on `SearchResultSimilarityBadge.vue`'s scale, not a distance: keep it in step with
 * that formula (`semanticSearch.ts#maxDistanceForMinMatch` inverts it). `0` means no floor.
 */
```

Reason: keep-in-sync constraint the code relies on but does not enforce: the stored unit is the badge's percentage, and the backend conversion must stay the inverse of the frontend formula.
