# wp-3717 comment recommendations

## backend/api/tasks.ts, `routes` (GET /sites/:siteId/tasks)

- **Add** above the route schema:
  ```
  // -> No route-level `permissions`: `read:pages` is a page rule permission, which that hook cannot
  //    check — decided per page by `models/tasks.ts#listOpenTasks`.
  ```
- Reason: root CLAUDE.md asks a page-scoped route to say so with a `No route-level permissions:` comment; it is a constraint the code relies on but does not enforce.
