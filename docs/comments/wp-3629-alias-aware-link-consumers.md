# WP 3629: comment recommendations (`backend/models/navigation.ts`)

No comments were added or edited in code. Recommended, for review:

- `localeRoutingKey`: `// -> The generated tree bakes each page's link target into the cached items, so a change to forcePrefix, aliases or the primary locale must miss the cache rather than serve the old spelling. Nothing else invalidates on a site locale-config edit.`
