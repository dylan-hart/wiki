# WP 3728 comment recommendations

## frontend/src/components/InboxOverlay.vue, `props` doc comment

**Change**

Current:

```
Initial state from whoever opened this overlay (`siteStore.openOverlay('Inbox', opts)`): `tab`
picks which of the two sections opens; `submissionId`/`from` are `InboxReview`'s own initial
state, passed straight through.
```

Proposed:

```
Initial state from whoever opened this overlay (`siteStore.openOverlay('Inbox', opts)`): `tab`
picks which section opens; `submissionId`/`from` are `InboxReview`'s own initial state, passed
straight through.
```

Reason: the overlay now has three sections, so "the two" contradicts the code.

## frontend/src/components/InboxOverlay.test.js, `mountInboxOverlay` `stores` comment

**Change**

Current: `Both tabs' content fetches off siteStore.id on mount; seeded so those calls take their real path instead of the "no site yet" guard clause.`

Proposed: `Each tab's content fetches off siteStore.id on mount; seeded so those calls take their real path instead of the "no site yet" guard clause.`

Reason: there are three tabs now, so "Both" is stale.
