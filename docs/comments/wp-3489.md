# WP #3489 — recommended comment changes

## `backend/models/siteImport.ts`, `importSite`

Delete the `FIXME:` block directly above `const mappedAssetRows = assetManifest.map(...)`:

```
// FIXME: `Promise.all` reads every blob `readArchive` staged to disk back into memory at once,
//        undoing that staging — read each chunk's blobs inside the insert loop below instead.
```

It is resolved: `mappedAssetRows` now holds metadata and staged file paths only, and the asset insert loop reads
each `ASSET_INSERT_CHUNK_SIZE` batch's blobs just before its insert. Nothing true remains in the marker.

Optional replacement (a real constraint the code relies on but does not enforce), placed above the asset
insert loop:

```
// -> Blobs are read per chunk, inside the transaction, so peak memory is one chunk's worth rather than
//    the whole archive; an unreadable staged file therefore aborts (and rolls back) the restore.
```
