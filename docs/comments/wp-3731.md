# WP 3731 comment recommendations

## frontend/src/helpers/treeNodes.js, `fetchTreeEntries`

**Add** (one more line in the existing JSDoc `@param` list):

```
 * @param {string} [params.orderBy] A `TREE_ORDER_BY` value; `sortOrder` by default, so every reader sees manual order.
```

Reason: the default is what makes manual order apply in the file manager, link picker and tree browser alike, and the signature alone does not say it.

## frontend/src/helpers/treeNodes.js, `reorderableIds` and `idsWithFolderOrder`

**Add** (JSDoc above `reorderableIds`):

```
/**
 * The ids `PUT sites/:siteId/tree/order` wants: pages and folders only (assets are not ordered),
 * with a page and a folder sharing a `fileName` adjacent, since the route gives the pair one position.
 */
```

Reason: why assets are absent and why twins are regrouped cannot be re-derived from the code.

## frontend/src/components/TreeLevel.vue, `handleReorder`

**Add** (above the function):

```
// -> Mutates `nodes`/`roots` in place: SortableJS has already moved the DOM, so the data has to follow or the next patch fights it
```

Reason: the optimistic write is load-bearing and looks redundant next to the emitted event.

## frontend/src/components/FileManager.vue, `listSortableOptions`

**Add** (above the computed):

```
// -> `forceFallback`: native drag would trip `.fileman-droptarget`'s upload handlers
```

Reason: the option's cause is invisible from the code.
