# backend/api/pages/export.ts

Delete the `FIXME:` marker above the PDF route's `Content-Disposition` header (the two lines starting "name the file through `exportFilenameStem(page.path)`"). The PDF download is now named through `exportFilenameStem`, so the marker describes a defect that no longer exists.
