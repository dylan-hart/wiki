# WP 3773 comment recommendations

## frontend/src/composables/quickNote.js, `isQuickNoteChord`

**Add** (JSDoc above the function):

```
/**
 * Mod+Alt+N. With Cmd it matches `code`, since Option+N is a dead key on macOS and `key` reads
 * `Dead`/`˜`. With Ctrl it matches the typed `key`, because Windows reports AltGr as Ctrl+Alt: a
 * layout where AltGr+N types a character (Polish `ń`) is typing, not the shortcut.
 */
```

Reason: the reader can't tell from the code alone why the two branches key off different fields,
and "fixing" either one to match the other brings back a real layout bug.

## frontend/src/components/HeaderNav.vue, the template comment above `<header-actions-menu>`

**Change**

Current:

```
<!-- One button for the five: an icon whose meaning is only in a tooltip is nothing a touch
     screen can offer, and by 900px they are crowding the site title too -->
```

Proposed:

```
<!-- One button for the row: an icon whose meaning is only in a tooltip is nothing a touch
     screen can offer, and by 900px they are crowding the site title too -->
```

Reason: the count is a fragile specific, and it is now wrong because the Quick Note button makes
six.

## frontend/src/pages/AdminGeneral.vue, the `commit` payload's `features` object

**Add** (above the `features:` key in `commit`):

```
// FIXME: `features.collaborativeEditing` has a toggle on this page but is never sent, so changing
//        it is silently dropped on save.
```

Reason: a real defect found while reading. The "Allow Collaborative Editing" toggle is bound to
`state.config.features.collaborativeEditing`, but the PUT payload lists every other feature flag and
leaves this one out.
