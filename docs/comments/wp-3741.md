# Comment recommendations, WP 3741

No existing comment was made stale by this change. The three below are additions I did not make.

## `backend/api/assets.ts`, route `GET /sites/:siteId/assets/search`

**Add**, inside the route options object, above `schema`:

```ts
/* No route-level `permissions`: as for the upload route above. */
```

Reason: root CLAUDE.md asks every route that checks a rule-based permission in its handler to say
so; the sibling asset routes carry this line. Here the check lives in `searchAssets()` (through
`mayActorOnAsset`), not in the handler body.

## `backend/models/assets.ts`, `setSearchContent`

**Add**, as a doc comment:

```ts
/**
 * Leaves `updatedAt` alone: it is the served file's ETag, and this changes no byte of it. A blank
 * `text` clears the vector too, so a file whose extraction found nothing stops matching.
 */
```

Reason: the missing `updatedAt` bump looks like an omission next to every other write in this file.

## `backend/modules/search/db/assetSearch.ts`, the `mayHoldPermissionSomewhere` early return

**Add**:

```ts
// -> A coarse hint only: `mayActorOnAsset` below is the gate, per row.
```

Reason: `mayHoldPermissionSomewhere` documents that it is unsafe as the sole gate, and this call site
should say it is not one.
