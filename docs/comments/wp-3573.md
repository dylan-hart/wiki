# WP #3573 comment recommendations

## backend/controllers/metrics.ts

- Header comment, last paragraph: "so bearer verification and the `manage:system` check are repeated
  below" is now stale. Suggested: "so bearer verification and the permission check (`manage:system` or
  `read:metrics`) are repeated below."
- Add above the `METRICS_PERMISSIONS.includes` check, one line: `apiKey.permissions` is already narrowed
  to the key's scope, so accepting either permission cannot widen a scoped key.
