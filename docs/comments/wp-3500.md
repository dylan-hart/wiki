# WP #3500 — recommended comment and doc changes

`storage.state` no longer exists in the 3.0 schema. Comments and docs below still describe it.

## `backend/db/schema.ts` (`storage` table)

Delete the `// TODO: drop -- dead column, nothing reads or writes it. Held a removed setup wizard's state.`
line. It now sits directly above `siteId` and refers to a column that is gone.

## `backend/migration/mappers/storage.ts`

Owned by #3481, so not touched here. Two spots describe a 3.0 `state`:

- The header text near line 37 ("`state` is left untouched for want of any transform at all: ...") should say
  `state` is a 2.x-only column that 3.0 has no counterpart for, rather than implying a 3.0 target.
- The `SourceStorageRow.state` field comment near line 54 ("Never mapped: 3.0's own `state` shape has no 2.x
  counterpart.") should read "Never mapped: 3.0 has no `storage.state` column."

## `docs/migration/2.5x-to-3.0-mapping.md` (storage table, `state` row near line 516)

Replace the row's target and note. Suggested:

| `state` | (none) | **not carried over** — 2.x's `state` was module-defined free-form json held for a since-removed setup wizard; 3.0 has no `storage.state` column, and the importer never mapped it |
