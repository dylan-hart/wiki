# Comment recommendations: WP #3817

## `frontend/src/pages/ProfileAuth.test.js`: the block comment above `vi.mock('@/composables/dialog', ...)`

**Change**

Current:

> The write-action tests need `confirm(...).onOk(cb)` to fire its callback immediately rather than
> wait on a rendered WConfirmDialog. No other test here clicks into a menu item that reaches
> `confirm()`/`dialog()`, so mocking both file-wide is safe.

Proposed:

> The write-action tests need `confirm(...).onOk(cb)` to fire its callback immediately rather than
> wait on a rendered WConfirmDialog.

Reason: the second sentence is no longer true. The "disconnect a sign-in method" tests click the
Disconnect menu item, which reaches `confirm()`, and they depend on the mock.

## `frontend/src/pages/ProfileAuth.test.js`: the comment in "adds a passkeys card once there is a passkey, ..."

**Remove**

Current:

> `mountWithApp` rather than this file's own `mountPage`: a passkey row renders its created date
> through `humanizeDate()`, which reads `userStore` and so needs a Pinia instance.

Reason: `mountPage` now goes through `mountWithApp` too, because `ProfileAuth.vue` reads
`useSiteStore()`. The contrast the comment draws no longer exists.
