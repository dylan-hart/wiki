# Scope decided in OpenProject task 666 (Feature 402): multi-format page import is built as
# described below (tasks 667/668), alongside PDF/diagram export in puppeteer/definition.yml.
key: pandoc
title: Pandoc
description: >-
  Converts between markup formats. Backs page import (single-file and multi-file batch) from
  MediaWiki, Textile, DocBook, reStructuredText, docx and odt into the markdown editor's format
  (models/import.ts's convertToMarkdown()).
website: 'https://pandoc.org'
detect:
  type: command
  value: pandoc
isInstallable: false
