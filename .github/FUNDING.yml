# Inherited from upstream Wiki.js and not yet replaced: every target below funds the upstream
# Wiki.js maintainers (NGPixel / Requarks), not Cardinal.js. Deliberately left naming upstream
# rather than renamed -- a renamed copy would claim these are Cardinal.js's, which they are not.
# Tracked in docs/audits/legal/README.md, "What is not clean".

github: [NGPixel]
patreon: requarks
open_collective: wikijs
ko_fi:
tidelift: # <platform-name>/<package-name>, e.g. npm/babel
custom:
